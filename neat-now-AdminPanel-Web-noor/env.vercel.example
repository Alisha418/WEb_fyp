# Vercel → Project → Settings → Environment Variables
# Use relative /api (proxied to EC2 in vercel.json). Do NOT use http://13.x.x.x here on HTTPS — browser blocks mixed content.

VITE_API_URL=/api

# Copy remaining VITE_FIREBASE_* from your local .env
